# 不进镜像：版本库、本地状态、密钥、文档与测试
.git
.gitignore
node_modules

# 运行时状态——部署时挂到 GCS 卷，绝不烤进镜像（也避免把本地真实记录带上云）
data
prototype/generated
logs
*.log

# 密钥绝不进镜像（云上走环境变量 / Secret Manager）
.env
.env.*

# 运行时用不到
docs
test
.claude
*.md
